# Operator values — the only file you edit. Copy to .env and adjust.
# Everything network-related (genesis, peers, image pins) is read from the
# artifacts directory by setup.sh; nothing here changes when the network
# changes.

# REQUIRED. Path to the network artifacts directory published by the
# network operator — the folder holding genesis.json, p2p.yaml, SHA256SUMS
# and friends.
NETWORK_DIR=

# Local ports. Both bind to loopback only — they answer on this host and
# nowhere else.
EL_RPC_PORT=8545
CL_HTTP_PORT=5052

# OPTIONAL: serve the RPC from behind NAT through a tunnel server.
# Uncomment the profile line to enable; the three TUNNEL_* values come from
# the tunnel server operator. The token is a secret — keep it out of shell
# history and pasted logs.
#COMPOSE_PROFILES=tunnel
#TUNNEL_SERVER_ADDR=
#TUNNEL_AUTH_TOKEN=
#TUNNEL_REMOTE_PORT=

# Local port of the tunnel client's status endpoint (loopback only).
#TUNNEL_STATUS_PORT=7400
