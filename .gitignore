# Operator-local configuration and secrets
.env
secrets/

# Fetched network artifacts and files rendered from them
artifacts/
compose.override.yaml
.setup-state/

# Dependencies of the marketplace package (installed by npm ci)
marketplace/node_modules/

# IDE / editor
.idea/
.vscode/

# Claude Code local settings
.claude/settings.local.json*

# Rendered tunnel client config (regenerated by setup.sh)
frpc.toml
